#include <stdio.h>
#include <stdlib.h>

int main()
{
    int age = 40;
    double gpa = 3.60;
    char grade = 'A';
    char string[] = "What's up"; /* Array of char types, but cannot be
                                    modified when tried to within the method after declaration*/
                                    
    return 0;
}